# Backend configuration for the dev environment.
#   terraform init -backend-config=environments/dev.s3.tfbackend
#
# Create the bucket first with the bootstrap/ root module.

bucket       = "tech-challenge-tfstate-841845498000"
key          = "dev/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
